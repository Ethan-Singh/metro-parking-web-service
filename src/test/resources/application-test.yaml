---
spring:
  application:
    name: metro-parking-web-service
external-server:
  parking:
    base-url: http://localhost:8081
    api: /parking
    apikey: test-key
    policy:
      disabled-facilities:
        - 1
        - 2
      backfill-window: 31
    security:
      allowed-origins:
        - http://localhost:3000
      rate-limit:
        capacity: 25
        refill-tokens: 10
        refill-period-seconds: 60